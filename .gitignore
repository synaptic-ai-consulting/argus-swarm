node_modules/
dist/
*.log
.env
.env.local
*.db
.argus/
# Local backlog only — never commit. (Pattern `plans/` matches a `plans` directory anywhere.)
# If it was tracked: git rm -r --cached plans/
plans/
/STATUS.md
